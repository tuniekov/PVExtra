export default {
    // Пункты меню менеджера MODX. Ставятся установщиком gtsAPI так же, как сниппеты.
    //
    // Своя страница в админке НЕ требует своего контроллера: пункт можно повесить
    // на страницу gtsAPI (namespace 'gtsapi', action 'home'), а что на ней показать —
    // описать в params. Формат тот же, что у страниц сайта:
    //   &config={"<Сниппет>": {<его параметры>}}
    //
    // Ключ объекта — имя пункта; оно же становится подписью, если не задан text.
    // PK у modMenu — text, поэтому пункт с тем же названием обновится, а не задвоится.
    //
    // exampleMenu:{
    //     text:'Название пункта',
    //     description:'Подсказка под названием',
    //     action:'home',              // контроллер; для страницы gtsAPI — 'home'
    //     namespace:'gtsapi',         // чей контроллер; по умолчанию — имя пакета
    //     params:'&config=myapp_admin',
    //     parent:'components',        // раздел меню, по умолчанию «Компоненты»
    //     menuindex:0,
    //     icon:'',
    // },
    //
    // ⚠️ В params можно передать ТОЛЬКО имя системной настройки, где лежит JSON.
    // Сам JSON туда писать нельзя: меню менеджера MODX рендерится через Smarty,
    // и фигурные скобки он разбирает как свой тег — страница менеджера падает
    // с «Smarty Compiler: Syntax error in template ... Unexpected ":"».
    //
    // То есть в settings.js:
    //   'admin': { xtype:'textarea', value:'{"mixVue":{"app":"myapp","config":{"module":"Main"}}}' }
    // и здесь: params:'&config=myapp_admin'.
    //
    // Что можно описать в этом JSON (см. страницу gtsAPI):
    //   таблица       {"PVTable":{"table":"MyTable"}}
    //   вкладки       {"PVTabs":{"tabs":{"MyTable":{"title":"Записи","table":"MyTable"}}}}
    //   приложение    {"mixVue":{"app":"myapp","config":{"module":"Main"}}}
    //
    // ⚠️ Меню удаляются НЕ автоматически: убрали пункт отсюда — на сайтах, где он
    // уже стоял, он останется. Удалять руками в «Меню» менеджера.
}
