/**
 * Свои группы пользователей пакета + права на его группы ресурсов.
 *
 * ⚠️ Сначала подумайте, нужна ли своя группа. Если задача — ограничить доступ к данным
 * или действиям, обычно правильнее шаблон разрешений MODX: см. permissions.js — он не
 * плодит сущностей и ложится на те группы, которые на сайте уже есть.
 *
 * Своя группа оправдана, когда пакету нужен закрытый раздел, куда админ просто
 * добавляет людей.
 *
 * Резолвер идемпотентен; при удалении пакета группы и права НЕ сносятся — в них уже
 * могут быть добавлены люди.
 *
 * Пример:
 *
 * export default {
 *     'MyGroup': {
 *         description: 'Доступ к разделу',
 *         resource_groups: ['mypackage_pages'],  // группы ресурсов из resource_groups.js
 *         policy: 'Resource',                    // политика доступа (по умолчанию Resource)
 *         context: 'web',
 *         authority: 9999,
 *     },
 * }
 */
export default {}
